{{!--

If the user is logged in , and 'GET /protected' endpoint is called, you will render this view based on the specs.

--}}

<main>
    <h1 class="title">Welcome!</h1>
    <p class={{{class}}}>Welcome {{firstName}} {{lastName}}, the time is now: {{currentTime}}. Your role in the system is {{role}}.</p>

    {{#if admin}}
        <a href="/admin"></a>
        {{/if}}
</main>
<footer>
    <a href="/logout">Log out</a>
</footer>